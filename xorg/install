#!/bin/bash
# Install local xorg configuration.
MODPATH=$(cd "$(dirname "$0")"; pwd)
ln -sf "${MODPATH}/../xmodmap/standard" ~/.xmodmap
ln -sf "${MODPATH}/Xresources" ~/.Xresources
ln -sf "${MODPATH}/xsessionrc" ~/.xsessionrc
