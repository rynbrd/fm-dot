#!/bin/bash
# Install bash dot files.
MODPATH=$(cd "$(dirname "$0")"; pwd)
ln -sf "${MODPATH}/bashrc" ~/.bashrc
ln -sf "${MODPATH}/bash_logout" ~/.bash_logout
ln -sf "${MODPATH}/bash_profile" ~/.bash_profile
ln -sf "${MODPATH}/inputrc" ~/.inputrc
